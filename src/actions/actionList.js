export const FilterAction = (data) => {
    // debugger;
    return {
        type: "FILTER_ACTION",
        data
    }
}

export const completeIssueFilter = (data) =>{
    // debugger;
    return {
        type: "COMPLETE_ISSUES",
        data
    }
}

